%h2 Sign up
= form_for(@user) do |f|
  = render 'shared/error_messages', object: f.object
  .field
    = f.label :user_name
    %br/
    = f.text_field :user_name
  %div
    Only a-z and 0-9 can be used.
  .actions
    = f.submit "Sign up"
